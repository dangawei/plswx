<button style="background-color:{{bgc}};box-shadow:{{bs}};border:{{borders}}" hover-class='btn-hover' data-num="{{btnNum}}" class="btn-class {{butType?'btn-type':'btn-type-no'}} size-{{size}} {{!isDisabled ? 'btn-disabled' : ''}} {{ghost && 'ghost'}} {{clickBool ? 'click' : ''}}" bindtap='btnClick' disabled="{{!isDisabled}}">
  <text class="btn-text">{{titleOne?titleOne:''}}</text>
  {{titleTwo?titleTwo:btnTitle}}
</button>
